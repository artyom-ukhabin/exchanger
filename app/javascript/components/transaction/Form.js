import React from "react"
import PropTypes from "prop-types"

class Form extends React.Component {
  render () {
    return (
      <React.Fragment>
        Hi there
      </React.Fragment>
    );
  }
}

export default Form
